name: 'lone_star_dbt'
version: '1.0.0'
config-version: 2

profile: 'lone_star_dbt' # <--- MUST MATCH profiles.yml

model-paths: ["models"]

models:
  lone_star_dbt:
    +materialized: table # This tells dbt to create actual tables in Snowflake